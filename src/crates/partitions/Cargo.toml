[package]
name = "tx-indexer-partitions"
version = "0.1.0"
edition = "2024"

[dependencies]
tx-indexer-primitives = { path = "../primitives" }
bitcoin = { workspace = true }

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
